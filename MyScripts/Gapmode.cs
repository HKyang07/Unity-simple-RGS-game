using UnityEngine;
using System.Collections;

public class Gapmode : MonoBehaviour {

    bool mode = true;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    void click()
    {
        mode = !mode;
        if (mode)
        {
        }
        else
        {
            
        }
    }
}
